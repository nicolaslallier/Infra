.env
.env.bak
.portainer.env
.runner.env
.openbao.env
certs/*
!certs/.gitkeep
# OpenBao's auto-unseal key: 32 raw bytes that decrypt the openbao-data
# volume. Generated by `make seal-key`, backed up off this machine, never
# committed.
openbao/seal.key
.DS_Store
# Scratch d'agents : contient des copies completes de .env (mots de passe).
Claude outputs/
*.env
__pycache__/
