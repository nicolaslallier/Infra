server {
    listen 443 ssl;
    server_name airflow.infra.famillelallier.net;

    include /etc/nginx/snippets/ssl.conf;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream http://airflow-apiserver:8080;
        proxy_pass $upstream;
        include /etc/nginx/snippets/proxy.conf;
    }
}
