# Internal-only stub_status for nginx-exporter (infra-net). Not published
# on the host — no ports: mapping for 8080 on the nginx service.
server {
    listen 8080;
    server_name _;

    location /stub_status {
        stub_status;
        access_log off;
    }
}
