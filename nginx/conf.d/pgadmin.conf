server {
    listen 443 ssl;
    server_name pgadmin.famillelallier.net;

    include /etc/nginx/snippets/ssl.conf;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream http://pgadmin:80;
        proxy_pass $upstream;
        include /etc/nginx/snippets/proxy.conf;
    }
}
